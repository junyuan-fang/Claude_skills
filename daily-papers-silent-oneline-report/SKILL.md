---
name: daily-papers-silent-oneline-report
title: 每日论文推荐三步流水线（静默 + 一句话回执）
description: 用户说「今日论文推荐」/「过去 N 天论文推荐」时，静默串跑 daily-papers 的 fetch→review→notes 三步流水线，全程不发中间状态消息，完成后只用一句话回报推荐文件路径、推荐/笔记篇数与目录页刷新状态
trigger_keywords: ["今日论文推荐", "过去 N 天论文推荐", "论文推荐", "论文推荐 静默", "daily-papers 一句话", "daily-papers", "论文流水线", "三步流水线", "论文抓取", "论文点评", "批量笔记", "静默流水线", "静默"]
source: date=2026-06-15
version: 7
updated_at: 2026-09-05T00:00:00
---

# 论文流水线静默运行 + 一句话汇报

## 何时使用

用户说「今日论文推荐」「过去 N 天论文推荐」「最近 5 天论文」「看看这周有啥论文」等，希望一次跑完整条流水线，且不想被中间过程刷屏。

## 步骤

1. 用户说「今日论文推荐」/「过去 N 天论文推荐」时，**直接进入 `daily-papers` 总入口**，不要逐步征询确认、不要先问要不要跑。
2. 顺序执行三步流水线，中途**不发任何中间状态消息**（不报「抓取完成」「第 1 步完成」「已完成 Step 1」「正在抓取」「开始打分」「正在生成笔记」，也不发中间产物路径）：
   - **fetch**：调用 `daily-papers-fetch` — 抓 arXiv + HuggingFace 最新论文，打分筛选、富化，输出 `/tmp/daily_papers_enriched.json`。多天模式按用户措辞把天数参数透传给这一步（「过去 3 天」「最近一周」「最近 5 天」「过去一周的论文」）。
   - **review**：调用 `daily-papers-review` — 读富化数据 + 扫描 Obsidian 笔记库，生成有态度的推荐点评，写入推荐文件 `/home/xinmiao/code/claude_bot/Obsidian_Vault/DailyPapers/$(date +%F)-论文推荐.md`，并更新 history。git 自动化保持关闭。
   - **notes**：调用 `daily-papers-notes` — 为「必读」档论文补概念库并生成完整笔记，链接回填到推荐文件，刷新目录页（概念 MOC / 论文 MOC）。git 自动化保持关闭。
3. **三步之间有严格数据依赖，必须串行执行，不能把 fetch/review/notes 三步并行。** 也不要在中途询问是否继续下一步——三步是一个整体。
4. 单步内部的笔记生成可用后台 agent 并行跑；**必须等每个 agent 回报并核对行数/校验结果**，确认无遗留项后才收尾。
5. 笔记默认**只为「必读」档生成**，不要扩到「值得看」档。
6. **逐篇校验产出**：确认每篇必读笔记文件确实写盘、行数达标（≥500 行视为完整笔记，实测完整笔记可达 900+ 行），有缺失或行数不达标则补跑第 3 步（notes）后再收尾。
7. 全部完成后，只输出**一句话**汇报，必须包含三要素：
   - 推荐文件绝对路径（如 `/home/xinmiao/code/claude_bot/Obsidian_Vault/DailyPapers/YYYY-MM-DD-论文推荐.md`），附分档「N 篇，x 必读 / y 值得看 / z 可跳过」
   - 重点论文笔记生成篇数 + 概念库新增条数
   - 目录页是否刷新（概念 MOC 更新 X 篇、论文 MOC 是否同步 Y 篇）
8. 若当日无新 robotics 必读（HF Trending 多为 LLM/T2I 跨界、arXiv API 超时等），笔记数可为 0，但仍要诚实在这一句话中说明原因。

## 一句话模板

`推荐文件 \`<绝对路径>\`（N 篇，x 必读 / y 值得看 / z 可跳过 — <可选原因说明>，已笔记 M 篇），目录页已刷新（概念 MOC X 篇、论文 MOC Y 篇）。`

## 笔记质量校验

- 生成的笔记必须包含 `## 关键公式` 与 `## 实验结果` 两个章节，缺失则**补全后再汇报完成**，不要带着缺章节的笔记收尾。
- 笔记需通过行数 / 公式 / 图表校验后才算完成（行数门槛见步骤 6）。
- **「笔记生成完毕」以文件回执（文件存在 + 行数达标）为准，不以 skill 或后台 agent 自报完成为准。** 拿到回报后要用行数或文件校验逐篇确认（例如确认某篇笔记 966 行、校验通过）。
- 汇报前确认推荐文件里的笔记链接已回填、目录页（概念 MOC / 论文 MOC）已刷新，不需要用户再手动改。

## 注意

- **git 自动化默认关闭**，三步全程不要自作主张 commit / push，除非用户明确要求提交。
- 不要在流水线中间输出进度播报（包括「第 1 步完成」之类）；用户明确要求「全程静默」，只要最终一句话结论，任何进度播报都算噪音。
- 整条流水线耗时约 25–30 分钟（实测 ~1780s），静默期不代表不工作，长响应时间属正常；不要中途插话或提前汇报。
- arXiv API 偶发超时是常见现象，HF Trending 是 fallback 源。
- 不要为了凑数把跨界 LLM/T2I 论文硬塞进 robotics 推荐。
- 一句话汇报里可顺带带上分级统计（必读 / 值得看 / 可跳过篇数）和概念库新增条数，但不要展开成多段。
